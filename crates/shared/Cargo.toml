[package]
name = "shared"
version = "0.1.0"
edition.workspace = true

[dependencies]
chat-domain = { path = "../chat_domain" }

serde.workspace = true
serde_json.workspace = true

uuid.workspace = true
