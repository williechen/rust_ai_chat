[package]
name = "desktop-pet"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
pet-domain = { path = "../../crates/pet_domain" }
tauri = { version = "2.11", features = [] }

[build-dependencies]
tauri-build = { version = "2.6", features = [] }
